export interface RpcRequest<T> {
    jsonrpc: string;
    method: string;
    params: T[];
    id: number;
}
export declare abstract class BaseRpcRequest<T> implements RpcRequest<T> {
    jsonrpc: string;
    abstract method: string;
    params: T[];
    id: number;
    constructor(params: T[], id?: number);
}
//# sourceMappingURL=rpc-request.type.d.ts.map