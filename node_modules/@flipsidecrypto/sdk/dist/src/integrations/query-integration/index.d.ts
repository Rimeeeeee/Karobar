import { Query, QueryResultSet, Filter, SortBy, QueryRun, SqlStatement, CompassApiClient } from "../../types";
export declare class QueryIntegration {
    #private;
    constructor(api: CompassApiClient);
    run(query: Query): Promise<QueryResultSet>;
    getQueryResults({ queryRunId, pageNumber, pageSize, filters, sortBy, }: {
        queryRunId: string;
        pageNumber?: number;
        pageSize?: number;
        filters?: Filter[];
        sortBy?: SortBy[];
    }): Promise<QueryResultSet>;
    createQueryRun(query: Query): Promise<QueryRun>;
    getQueryRun({ queryRunId }: {
        queryRunId: string;
    }): Promise<QueryRun>;
    getSqlStatement({ sqlStatementId }: {
        sqlStatementId: string;
    }): Promise<SqlStatement>;
    cancelQueryRun({ queryRunId }: {
        queryRunId: string;
    }): Promise<QueryRun>;
}
//# sourceMappingURL=index.d.ts.map